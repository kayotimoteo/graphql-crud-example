apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tft-webpage.fullname" . }}-test-connection"
  labels:
    {{- include "tft-webpage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tft-webpage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
