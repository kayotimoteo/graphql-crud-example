apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "010graphql.fullname" . }}-test-connection"
  labels:
    {{- include "010graphql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "010graphql.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
