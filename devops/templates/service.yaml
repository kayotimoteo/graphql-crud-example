apiVersion: v1
kind: Service
metadata:
  name: {{ include "010graphql.fullname" . }}
  labels:
    {{- include "010graphql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "010graphql.selectorLabels" . | nindent 4 }}
