apiVersion: v1
kind: Service
metadata:
  name: {{ include "tft-webpage.fullname" . }}
  labels:
    {{- include "tft-webpage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tft-webpage.selectorLabels" . | nindent 4 }}
